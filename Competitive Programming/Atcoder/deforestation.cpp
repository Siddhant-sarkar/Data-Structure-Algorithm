#include<iostream>
#include<vector>
using namespace std;

int countMinCost(vector<int>&arr){
	
}
int main(){
	int n;cin>>n;
	vector<int>arr(n);
	for(auto &it:arr) cin>>it;
	cout<<countMinCost(arr)<<endl;
}